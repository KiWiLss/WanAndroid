<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="@dimen/m168"
        android:background="@color/red"
>

    <de.hdodenhof.circleimageview.CircleImageView
            android:id="@+id/civ_home_header"
            android:layout_width="@dimen/m100"
            android:layout_height="@dimen/m100"
            android:src="@drawable/header"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:layout_marginTop="@dimen/m30"
            android:background="@drawable/bg_withe_ring"
            android:padding="@dimen/m2"
    />

    <TextView
            android:id="@+id/tv_home_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/app_name"
            app:layout_constraintTop_toBottomOf="@+id/civ_home_header"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            android:textSize="@dimen/m14"
            android:textColor="@color/white"
            android:layout_marginTop="@dimen/m10"/>

</android.support.constraint.ConstraintLayout>